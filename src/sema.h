// TODO(soimn): What should be done during this stage?
// * using import resolution
// * const if and defer handling
// * generate type table
// * check type of variables
// * check for undeclared and duplicated symbols
// * link all functions internally
// * check for type compatibility
// * check type of functions and handle arguments and default arg. values
// * check sequence expressions